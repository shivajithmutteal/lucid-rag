{
  "name": "@lucid-rag/providers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Concrete provider adapters for @lucid-rag/core: OpenAI-compatible embeddings + generation (OpenAI / Voyage / Groq / Gemini / OpenRouter / Ollama), resolved from env.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lucid-rag/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
