{
  "name": "@lucid-rag/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-agnostic RAG engine: contracts, chunking, hybrid retrieval + rerank, generation, eval, and the observable trace. Zero runtime dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
